meta {
  name: Get Store Events
  type: http
  seq: 3
}

post {
  url: {{API_URL}}/api/v3/events?lang={{lang}}
  body: text
  auth: none
}

params:query {
  lang: {{lang}}
}

headers {
  X-Token: {{API_TOKEN}}
  Token-Type: {{TOKEN_TYPE}}
  Accept: application/json
  Content-Type: text/plain
}

body:text {
  {{jwt}}
}

docs {
  getStoreEvents
}
