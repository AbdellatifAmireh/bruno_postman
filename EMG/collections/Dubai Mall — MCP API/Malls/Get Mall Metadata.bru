meta {
  name: Get Mall Metadata
  type: http
  seq: 3
}

get {
  url: {{API_URL}}/api/v1/malls/1/metadata?lang={{lang}}
  body: none
  auth: none
}

params:query {
  lang: {{lang}}
}

headers {
  X-Token: {{API_TOKEN}}
  Token-Type: {{TOKEN_TYPE}}
  Accept: application/json
}

docs {
  getMallMetadata — per-page SEO metadata.
}
